package com.example.SwiftBid.model.enums;

/**
 * Enum representing the roles a user can hold. A user may hold more than one
 * role at the same time (e.g. USER + SELLER), see {@link com.example.SwiftBid.model.Role}.
 */
public enum RoleName {
    USER,
    SELLER,
    ADMIN
}
